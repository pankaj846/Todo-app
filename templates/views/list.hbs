{{>header}}

<div class="box" id="heading">
  <h1>
    {{listTitle}}
  </h1>
</div>

<div class="box">

  {{#each newListItems}}
  <form action="/delete" method="post">
    <div class="item">
      {{!-- <input type="checkbox" name="checkbox" value="{{this._id}}" onChange="this.form.submit()"> --}}

      <button style="background-color: white; color: #003666; border: none;" type="submit" name="checkbox"
        value="{{this.id}}" name="list"><i style="font-size: 22px;" class="fas fa-trash"></i></button>
      <p>
        {{this.name}}
        <!-- <%=item.name%> -->
      </p>
    </div>
    <input type="hidden" name="listName" value="{{ listTitle }}"></input>
  </form>
  {{/each}}


  {{!-- for loop --}}
  {{!-- {{#each newListItems}}
  <div class="item">
    <input type="checkbox">
    <p>{{this}}</p>
  </div>
  {{/each}} --}}

  <form class="item" action="/" method="post">
    <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
    <button style="margin-right: 20px; background-color: white; color: #003666; font-size: 39px; border: none;"
      class="addtn" type="submit" name="list" value="{{listTitle}}"><i class="fas fa-plus-circle"></i></button>
    {{!-- <button class="addbtn" type="submit" name="list" value="{{listTitle}}">+</button> --}}
  </form>
</div>

{{>footer}}